#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "upgrade" ]; then
    if systemctl is-active --quiet mmorpg-gatekeeper.service; then
        systemctl stop mmorpg-gatekeeper.service || true
    fi
fi
if [ "$1" = "remove" ]; then
    systemctl disable mmorpg-gatekeeper.service || true
fi
exit 0
